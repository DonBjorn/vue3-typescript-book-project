import { {{ pascalCase name }}sStateInterface} from "@/models/store";

/**
 * @name initial{{ pascalCase name }}sState
 * @description
 * La instancia del estado de {{ pascalCase name }}s con los valores por defecto
 * Generado automáticamente con Plop.js
 */
export const initial{{ pascalCase name }}sState: {{ pascalCase name }}sStateInterface = {
    loading: false,
    {{ camelCase name }}s: []
}
